# Allowlist pattern: ignore everything, then permit only what should be tracked.
# This prevents runtime artifacts, dolt databases, backups, and other local-only
# files from ever being committed — even as beads adds new file types.

# Ignore everything
*

# Allow these files (the source of truth for beads)
!.gitignore
!README.md
!config.yaml
!issues.jsonl
