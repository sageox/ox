# Improve ox status output

## Status table

Render the sync state as a Tufte table in the terminal. Show repo, branch, and
ledger freshness columns. Use the existing styles, no new colors.

## JSON mode

Add a --json flag that emits the same data as structured output for agents.
