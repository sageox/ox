---
title: "ox index github"
description: "CLI reference for ox index github"
sidebar_position: 49
---

## ox index github

Index GitHub PRs and issues

### Synopsis

Fetch recent pull requests and issues from GitHub and make them searchable.

Data is extracted to the project ledger and indexed into CodeDB for search
with 'ox code search type:pr' or 'ox code search type:issue'.

Requires a GitHub token (GITHUB_TOKEN, GH_TOKEN, or gh CLI config).
Controlled by github_sync, github_sync_prs, and github_sync_issues
settings in .sageox/config.json (all enabled by default).

```
ox index github [flags]
```

### Options

```
  -d, --days int      max days of history to fetch (default 30)
      --full          clear sync state and re-fetch everything
  -h, --help          help for github
      --issues-only   index only issues
      --no-push       extract to ledger without pushing
      --prs-only      index only pull requests
```

### Options inherited from parent commands

```
  -c, --config string    config file path (default: .sageox/config.yaml)
      --json             output in JSON format (default: false)
      --no-interactive   disable spinners and TUI elements (auto-enabled when CI=true)
      --profile          generate CPU profile and execution trace for performance analysis (default: false)
  -q, --quiet            suppress non-error output (default: false)
  -v, --verbose          enable verbose output (default: false)
```

### SEE ALSO

* [ox index](/index)	 - Index code and project data for search
