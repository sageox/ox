# Agent version configuration for Docker test environments
# These versions are used as defaults when building test containers
# Update these when you want to test against specific agent versions

CLAUDE_CODE_VERSION=latest
CODEX_VERSION=latest
GEMINI_VERSION=latest
AMP_VERSION=latest
PI_VERSION=latest
OPENCODE_VERSION=latest
CODE_PUPPY_VERSION=latest

# Base environment versions
NODE_VERSION=22
GO_VERSION=1.24